<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="foster-base.list">
      <field name="name">foster-base list</field>
      <field name="model">foster-base.foster-base</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="foster-base.action_window">
      <field name="name">foster-base window</field>
      <field name="res_model">foster-base.foster-base</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="foster-base.action_server">
      <field name="name">foster-base server</field>
      <field name="model_id" ref="model_foster-base_foster-base"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="foster-base" id="foster-base.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="foster-base.menu_1" parent="foster-base.menu_root"/>
    <menuitem name="Menu 2" id="foster-base.menu_2" parent="foster-base.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="foster-base.menu_1_list" parent="foster-base.menu_1"
              action="foster-base.action_window"/>
    <menuitem name="Server to list" id="foster-base" parent="foster-base.menu_2"
              action="foster-base.action_server"/>
    -->
  </data>
</odoo>