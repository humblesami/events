<odoo>
    <data>
        <record id="meeting_point_groups_tree" model="ir.ui.view">
            <field name="name">meeting.point.res.groups.tree</field>
            <field name="model">res.groups</field>
            <field name="mode">primary</field>
            <field eval="27" name="priority"/>
            <field name="arch" type="xml">
                <tree string="Access Groups">
                    <field name="full_name" modifiers="{'readonly': true}"/>
                </tree>
            </field>
        </record>

        <record id="meeting_point_groups_form" model="ir.ui.view">
            <field name="name">meeting.point.res.groups.form</field>
            <field name="model">res.groups</field>
            <field name="mode">primary</field>
            <field eval="25" name="priority"/>
            <field name="arch" type="xml">
                <form string="Groups">
                    <sheet>
                        <group>
                            <field name="name" attrs="{'readonly':['|',('name', '=', 'Admin'),('name', '=', 'Staff')]}"/>
                        </group>
                        <notebook>
                            <page string="Users">
                                <field name="users" context="{'search_default_no_share':1}"/>
                            </page>
                            <page string="Access Rights">
                                <field name="model_access">
                                    <tree string="Access Rules" editable="bottom" create="false" delete="false">
                                        <field name="model_id"/>
                                        <field name="perm_read"/>
                                        <field name="perm_write"/>
                                        <field name="perm_create"/>
                                        <field name="perm_unlink"/>
                                        <field name="name"/>
                                    </tree>
                                    <form string="Access Controls">
                                        <group col="4">
                                            <field name="name"/>
                                            <field name="active"/>
                                            <field name="model_id"/>
                                            <newline/>
                                            <field name="perm_read"/>
                                            <field name="perm_write"/>
                                            <field name="perm_create"/>
                                            <field name="perm_unlink"/>
                                        </group>
                                    </form>
                                </field>
                            </page>
                            <page string="Notes">
                                <field name="comment"/>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="action_res_groups" model="ir.actions.act_window">
            <field eval="26" name="sequence"/>
            <field name="name">Meeting Roles</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.groups</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[('category_id.name','=','MeetingPoint')]</field>
            <field name="context">{'module_name' : 'meeting_point','mp_groups' : True}</field>
            <field name="help">A group is a set of functional areas that will be assigned to the user in order to give
                them access and rights to specific applications and tasks in the system. You can create custom groups or
                edit the ones existing by default in order to customize the view of the menu that users will be able to
                see. Whether they can have a read, write, create and delete access right can be managed from here.
            </field>
        </record>

        <record id="action_group_tree" model="ir.actions.act_window.view">
            <field name="view_mode">tree</field>
            <field name="view_id" ref="meeting_point_groups_tree"/>
            <field name="act_window_id" ref="action_res_groups"/>
        </record>

        <record id="actio_meeting_point_group_form" model="ir.actions.act_window.view">
            <field name="view_mode">form</field>
            <field name="view_id" ref="meeting_point_groups_form"/>
            <field name="act_window_id" ref="action_res_groups"/>
        </record>
    </data>
</odoo>